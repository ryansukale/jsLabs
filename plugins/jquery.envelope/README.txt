This plugin will take a given dom element and animate its border as if an envelope.
Requires

Depends jquery.curve.js
Works only in CSS3 compatible browsers.

The element shold have a border. If not, the widgett will add a border of 1 px to the element.

